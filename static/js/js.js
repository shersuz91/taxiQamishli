var bookForm = document.getElementById("bookForm")

bookForm.addEventListener("submit", function(event){
    // event.preventDefault();

    // var name = bookForm.querySelector("input[name = 'name']").value;
    // var phone = bookForm.querySelector("input[name = 'phone']").value;
    // var fromPlace = bookForm.querySelector("input[name = 'fromPlace']").value;
    // var toPlace = bookForm.querySelector("input[name = 'toPlace']").value;
    // var day = bookForm.querySelector("input[name = 'day']").value;
    // var note = bookForm.querySelector("textarea").value;
    // const message = `🚕  حجز جديد

    // الاسم: ${name}
    // الهاتف: ${phone}
    // من: ${fromPlace}
    // الى: ${toPlace}
    // الوقت: ${day}
    // الملاحظات: ${note}
    // `

    // const whatsappNumber = "14035889164";
    // const url = `https://api.whatsapp.com/send?phone=${whatsappNumber}&text=${encodeURIComponent(message)}`;

    // window.open(url, "_blank");
})
